const text = await response.text();

if (!text) {
  throw new Error("La API devolvió vacío");
}

let data;

try {
  data = JSON.parse(text);
} catch (e) {
  console.error("Respuesta NO es JSON:", text);
  throw new Error("La API no devolvió JSON válido");
}
